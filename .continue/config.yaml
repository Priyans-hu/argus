# Continue.dev Configuration
# Generated by Argus - https://github.com/Priyans-hu/argus
# Documentation: https://docs.continue.dev/reference

name: argus
version: "1.0.0"
schema: v1

# Rules are added to the system message for all requests
rules:
  - This is the argus project
  - "Primary technologies: Go 1.24"
  - "Frameworks in use: Cobra"
  - Use gofmt for formatting
  - "Handle all errors explicitly with if err != nil"
  - Write doc comments starting with the function name for exported functions
  - Prefer composition over inheritance
  - "Go-style explicit error checking (if err != nil)"
  - Test files use _test suffix (Go style)
  - Tests are colocated with source files
  - "Go project - use 'go fmt' or 'gofmt' for formatting"
  - Go doc comments (start with function name)
  - Use conventional style for commit messages
  - "Branch naming: <prefix>/<description>"

# Context providers supply additional information to the model
context:
  - provider: code
  - provider: diff
  - provider: terminal
  - provider: problems
  - provider: folder
  - provider: codebase

# Documentation sites to index for @docs context
docs:
  - title: Go
    startUrl: https://pkg.go.dev/std

